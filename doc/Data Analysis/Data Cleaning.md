# 数据清洗全流程（按标准工作流排序）
我按照**真实工作中数据处理的先后顺序**，重新整理了所有内容，逻辑更清晰、可直接跟着实操：

## 一、标准数据清洗流程（排序版）
### 1. 数据读取（第一步：加载数据）
读取本地/外部数据文件，生成数据表
- `pd.read_csv('文件路径')`
- `pd.read_excel('文件路径')`

### 2. 数据探索与描述（第二步：了解数据）
查看数据结构、基础统计信息，判断数据问题
- `df.info()`
- `df.describe()`

### 3. 基础格式清洗（第三步：统一数据格式）
修正最基础的格式错误
- 去除数据间的空格
- 英文字母大小写转换
- 去除字符串前后空格

### 4. 重复值处理（第四步：去重）
删除完全重复的记录，保证数据唯一性
- `duplicated()` 检测重复行
- `drop_duplicates()` 删除重复行

### 5. 缺失值处理（第五步：补全/删除空数据）
处理数据中空白、未记录的内容
- 删除缺失值
- 均值填补法
- 向前填充 / 向后填充
- 模型填补法（如随机森林）

### 6. 异常值处理（第六步：修正极端数据）
处理偏离正常范围的数值
- 删除异常值记录
- 当作缺失值处理
- 平均值修正、盖帽法修正
- 不处理（用于业务分析）

### 7. 文本字符串处理（第七步：清洗文本类数据）
统一文本格式、提取有效信息
- 去除特殊符号：`replace(',','')`
- 正则表达式提取所需数据

### 8. 时间格式序列处理（第八步：标准化时间数据）
统一时间格式，方便时间分析
- 系统时间格式化
- 系统时间与时间戳相互转换
- 提取年、月、日

---

## 二、行业术语注释（按流程对应排序）
| 流程步骤 | 专业术语 | 注释 |
|----|----|----|
| 数据读取 | pd.read_csv / pd.read_excel | pandas库读取CSV/Excel文件的函数，用于加载原始数据 |
| 数据探索 | df.info() | 查看数据表的列名、数据类型、非空数量等基础信息 |
| 数据探索 | df.describe() | 生成数据描述性统计（均值、最值、分位数等） |
| 重复值处理 | duplicated() | 检测数据表中重复的行 |
| 重复值处理 | drop_duplicates() | 删除数据表中的重复行 |
| 缺失值处理 | 缺失值 | 数据中未记录、为空的内容（常用 NaN 表示） |
| 缺失值处理 | 均值填补法 | 用列的平均值填充缺失值 |
| 缺失值处理 | 向前/向后填充 | 用前一个/后一个有效值填充空值（适合时间序列） |
| 缺失值处理 | 模型填补法 | 用机器学习模型（随机森林）预测并填充缺失值 |
| 缺失值处理 | 随机森林 | 常用机器学习算法，可用于缺失值预测、分类/回归 |
| 异常值处理 | 异常值 | 明显偏离正常范围的极端数值 |
| 异常值处理 | 盖帽法修正 | 将超出范围的异常值截断为安全阈值 |
| 文本处理 | replace() | 替换文本中的符号、字符 |
| 文本处理 | 正则表达式 | 提取、匹配、清洗复杂文本的工具 |
| 时间处理 | 时间戳 | 用数字表示时间，方便计算机存储与计算 |

---

### 总结
1. 流程严格遵循：**读取 → 探索 → 格式清洗 → 去重 → 缺失值 → 异常值 → 文本 → 时间**
2. 这是企业数据分析、数据挖掘、Python 数据处理的**标准工作流**
3. 注释全部对应实操步骤，方便理解和记忆

---
### 一、提取与整理信息
#### 章节标题
**3. 初步探索数据**

#### 代码与功能说明
| 代码片段 | 功能注释 | 输出/作用 |
|----------|----------|-----------|
| `df.shape` | 查看数据形状 | 输出结果：`(5100, 13)`，表示该数据集包含 **5100 行**（样本数）、**13 列**（特征数） |
| `df.info()` | 快速了解数据的结构 | 用于查看数据集的列名、数据类型、非空值数量等基础结构信息，通俗讲就是“提取关键词” |
| `df.describe()` | 快速查看数据的描述性统计信息 | 用于生成数值型列的统计摘要，包括均值、标准差、最小值、最大值、四分位数等 |

---

### 二、术语补充说明
- **`df`**：通常指 pandas 库中的 `DataFrame` 对象，是数据分析中最常用的二维表格数据结构。
- **`shape`**：`DataFrame` 的属性，返回一个元组 `(行数, 列数)`，直观反映数据集的规模。
- **`info()`**：`DataFrame` 的方法，可快速排查数据类型错误、缺失值分布等问题。
- **`describe()`**：`DataFrame` 的方法，帮助快速感知数值型数据的分布特征与异常范围。

---

# 数据清洗全流程关键词清单
我按照**标准数据处理流程**，把每个环节的核心关键词都整理出来了，方便你记忆和对照使用：

---

## 一、数据读取与加载
- `pd.read_csv()`
- `pd.read_excel()`
- `DataFrame`（简称 `df`）
- 文件路径
- 数据加载

---

## 二、数据探索与描述
- `df.shape`
- `df.info()`
- `df.describe()`
- 数据形状（行数、列数）
- 数据类型（int、float、object、datetime等）
- 非空值数量
- 缺失值
- 描述性统计（均值、标准差、最值、分位数）
- 数据结构

---

## 三、基础格式清洗
- 去除空格（前后空格、中间空格）
- 大小写转换
- `replace()`
- 格式统一

---

## 四、重复值处理
- `duplicated()`
- `drop_duplicates()`
- 重复行
- 去重
- 唯一值

---

## 五、缺失值处理
- 缺失值（`NaN`、`None`）
- 删除缺失值
- 均值填补法
- 向前填充（`ffill`）
- 向后填充（`bfill`）
- 模型填补法
- 随机森林
- 插值法

---

## 六、异常值处理
- 异常值
- 极端值
- 删除异常值记录
- 作为缺失值处理
- 平均值修正
- 盖帽法（截断法）
- 箱线图（检测异常值）
- 业务分析

---

## 七、文本字符串处理
- 文本清洗
- 去除前后空格
- `replace(',','')`
- 正则表达式
- 提取数据
- 特殊符号处理
- 字符串匹配

---

## 八、时间格式序列处理
- 时间格式化
- 时间戳
- 时间转换
- 提取年月日
- 日期解析
- `pd.to_datetime()`

---

## 九、数据清洗核心目标关键词
- 数据质量
- 数据完整性
- 数据一致性
- 数据准确性
- 数据唯一性
- 数据可用性

---

### 快速记忆小贴士
可以按这个口诀记：
**读 → 探 → 格 → 重 → 缺 → 异 → 文 → 时**
（读取 → 探索 → 格式 → 重复 → 缺失 → 异常 → 文本 → 时间）

---

